abstract class Card<T>
{
    constructor()
    {

    }
    element: HTMLElement;
}

//templateFunction: (a: T, index: number, addFunction: Function, menuFunctions: MoreMenuFunction[]) => HTMLElement, clipboardType?: ClipData, addFunction?: () => T